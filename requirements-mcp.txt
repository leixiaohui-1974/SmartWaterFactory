# MCP服务额外依赖

# HTTP模式支持
aiohttp>=3.9.0

# 异步支持（通常已包含在Python 3.7+）
# asyncio  # 标准库

# 可选：更好的性能
uvloop>=0.19.0; sys_platform != 'win32'

# 可选：WebSocket支持
aiohttp-sse>=2.2.0

# 日志增强（可选）
python-json-logger>=2.0.7
